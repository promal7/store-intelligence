# Custom tracker configuration bypassing external compilation requirements
tracker_type: botsort
track_high_thresh: 0.25
track_low_thresh: 0.1
new_track_thresh: 0.3
track_buffer: 30
match_thresh: 0.8
# 🚀 CRITICAL: Disabling appearance-based features prevents lap/lapx engine lookups
with_reid: False
